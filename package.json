{
  "name": "des-moines-wellness-center",
  "version": "1.0.0",
  "private": true,
  "description": "Des Moines Wellness Center \u2014 modern Next.js rebuild",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "check:links": "node scripts/check-links.mjs",
    "check:substances": "node scripts/check-substances.mjs",
    "check": "npm run check:links && npm run check:substances && npm run check:claims && npm run check:services",
    "check:claims": "node scripts/check-claims.mjs",
    "cutover:check": "node scripts/cutover-check.mjs",
    "verify:lead": "node scripts/verify-lead.mjs",
    "check:services": "node scripts/check-services.mjs",
    "check:numbers": "node scripts/check-campaign-numbers.mjs"
  },
  "//overrides": "postcss and sharp are pulled in transitively by next at versions with published high-severity advisories (postcss sourceMappingURL file disclosure; sharp/libvips CVE-2026-33327 et al). Both are patch/minor bumps within the same major. Re-check on each next upgrade and drop the pin once next ships the patched ranges itself.",
  "overrides": {
    "postcss": "8.5.25",
    "sharp": "0.35.3"
  },
  "dependencies": {
    "lucide-react": "0.469.0",
    "next": "15.5.22",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "@types/react": "19.0.7",
    "@types/react-dom": "19.0.3",
    "autoprefixer": "10.4.20",
    "postcss": "8.5.25",
    "tailwindcss": "3.4.17",
    "typescript": "5.7.3"
  }
}
